name: Test

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Debug workspace
        run: |
          pwd
          ls ${GITHUB_WORKSPACE}/..
